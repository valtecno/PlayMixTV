<?xml version="1.0" encoding="utf-8"?>
<!--
    Selector de canal de la multi-pantalla.

    Reemplaza al AlertDialog.setItems() anterior, que armaba un array con TODOS
    los nombres del panel: en el Sistema XL eso son decenas de miles de entradas
    y el diálogo se colgaba antes de dibujarse. Acá se usa un RecyclerView con
    filtro, que solo mantiene en pantalla las filas visibles.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/dialog_bg"
    android:padding="14dp">

    <TextView
        android:id="@+id/tvPickerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/text_light"
        android:textSize="17sp"
        android:textStyle="bold"
        android:layout_marginBottom="10dp" />

    <!-- ===== Buscador dedicado ===== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/bg_glass_card"
        android:paddingHorizontal="12dp">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_search"
            android:contentDescription="@string/multiscreen_search_hint" />

        <EditText
            android:id="@+id/etPickerSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:background="@null"
            android:hint="@string/multiscreen_search_hint"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:maxLines="1"
            android:textColor="@color/text_light"
            android:textColorHint="@color/text_muted"
            android:textSize="15sp" />

        <ImageView
            android:id="@+id/btnPickerClear"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:padding="6dp"
            android:src="@drawable/ic_backspace"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/multiscreen_search_clear"
            android:visibility="gone" />

    </LinearLayout>

    <!-- ===== Categorías: acotan la búsqueda sin bajar el catálogo entero ===== -->
    <HorizontalScrollView
        android:id="@+id/pickerCategoryScroll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/pickerCategoryContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal" />

    </HorizontalScrollView>

    <TextView
        android:id="@+id/tvPickerStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:textColor="@color/text_muted"
        android:textSize="13sp" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="6dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerPicker"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false" />

        <ProgressBar
            android:id="@+id/pickerProgress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

    </FrameLayout>

    <TextView
        android:id="@+id/btnPickerClose"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@drawable/bg_glass_card"
        android:gravity="center"
        android:padding="12dp"
        android:text="@android:string/cancel"
        android:textColor="@color/text_light"
        android:textSize="15sp"
        android:focusable="true"
        android:clickable="true" />

</LinearLayout>
